var reducer = require('../../src/reducers/emotion');

describe('emotion', () => {

  it('should not change the passed state', (done) => {

    const state = Object.freeze({});
    reducer(state, {type: 'INVALID'});

    done();
  });
});
